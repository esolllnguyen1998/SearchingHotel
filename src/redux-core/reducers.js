import { combineReducers } from "redux";
import { allHotelsReducer } from '../container/home-page/redux/reducer';

const rootReducer = combineReducers({
    allHotelsReducer
})

export default rootReducer;
